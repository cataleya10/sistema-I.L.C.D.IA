# Plan a 100% - 2026-02-14

Objetivo: cerrar los pendientes para declarar el sistema "100% listo para Go/No-Go".

## Estado actual validado hoy

- `validate-secrets.ps1`: OK
- `release-preflight.ps1 -SkipFrontendTests`: OK
- `release-preflight.ps1` completo con login y frontend: OK
- Build/tests backend: OK
- Tests frontend: OK
- Tests AI engine: OK
- Smoke runtime autenticado: OK
- Vulnerabilidades NuGet: sin hallazgos
- `check-operational-thresholds.ps1`: OK
- Backup/restore verificado: OK (`backups/20260214-082711` -> `restore-verify-20260214-082711`)
- CI/gobernanza remota: pendiente (sin token API; validacion manual GitLab requerida)
- Bloque BD productiva: pendiente (acordado al final)

## Por donde empezamos (estado)

Paso 1 ya cerrado hoy.

```text
Release preflight passed.
```

El siguiente frente activo es Paso 4 (CI/gobernanza) y luego Paso 5 (BD productiva).

Referencia del comando ya ejecutado:

```powershell
powershell -File scripts/release-preflight.ps1 `
  -ApiBase "http://localhost:5000" `
  -AiBase "http://localhost:8000" `
  -Username "<usuario>" `
  -Secret "<password>"
```

## Secuencia completa hasta 100%

1. Cerrar gate tecnico local completo (incluyendo frontend y login).
2. Ejecutar umbrales operativos con admin.
3. Ejecutar y guardar evidencia de backup/restore.
4. Cerrar governance y CI remoto (GitLab/GitHub). **Pendiente**
5. Cerrar bloque de base de datos productiva. **Pendiente**
6. Firmar decision Go/No-Go.

## Comandos por bloque

### 1) Gate tecnico local completo

```powershell
powershell -File scripts/release-preflight.ps1 `
  -ApiBase "http://localhost:5000" `
  -AiBase "http://localhost:8000" `
  -Username "<usuario>" `
  -Secret "<password>"
```

### 2) Umbrales operativos (admin)

```powershell
powershell -File scripts/check-operational-thresholds.ps1 `
  -ApiBase "http://localhost:5000" `
  -Username "<admin>" `
  -Secret "<password>" `
  -MaxServerErrors 0 `
  -MaxClientErrors 200 `
  -MaxAvgDurationMs 2500 `
  -MaxDurationMs 10000 `
  -MaxProcessingBacklog 200
```

### 3) Backup y restore

```powershell
powershell -File scripts/backup-operational.ps1 -IncludeLogs -IncludeEnv
powershell -File scripts/restore-operational.ps1 -BackupPath ".\backups\<timestamp>" -Overwrite
```

Evidencia minima:
- carpeta backup generada
- restore ejecutado sin errores

### 4) CI remoto + governance de rama

Si hay token y repo GitHub:

```powershell
powershell -File scripts/check-ci-pr-readiness.ps1 `
  -Repo "<owner/repo>" `
  -Branch "<branch>" `
  -GitHubToken "$env:GITHUB_TOKEN"
```

Si usan GitLab/UI:
- MR aprobado (>=1 aprobacion)
- sin conversaciones bloqueantes
- branch protegida
- pipeline en verde (security/backend/frontend/ai-engine)

### 5) Base de datos productiva (bloque final)

```powershell
powershell -File scripts/validate-secrets.ps1 -RequireDbConnectionString
```

Adicional obligatorio:
- `pg_dump` productivo
- restauracion de prueba en entorno aislado

### 6) Go/No-Go final

Documento de apoyo:
- `docs/release-checklist.md`
- `docs/release-readiness-2026-02-12.md`
- `docs/gitlab-manual-release-validation-template.md`

Criterio:
- Go solo con todos los bloques cerrados.
